# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
testproto.exe

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove these if you want to force people to run go mod download)
vendor/

# OS specific files
.DS_Store
Thumbs.db
